<html>
    <head>
        <style type="text/css">
            .centered {
                position: fixed;
                top: 50%;
                left: 50%;
                /* bring your own prefixes */
                transform: translate(-50%, -50%);
            }
        </style>
    </head>
    <body>
        <img id="monkeypic" src="{{ asset('img/monkey-drawing.jpg') }}" class="centered"/>
    </body>
</html>